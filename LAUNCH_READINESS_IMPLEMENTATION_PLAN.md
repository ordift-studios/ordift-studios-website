# Launch Readiness Implementation Plan

**Date:** 2026-08-05
**Source:** every finding in `PRODUCT_LAUNCH_READINESS_REVIEW.md`, grouped into four sprints and broken into individually implementable, individually verifiable tasks.
**Status:** **Sprint 1 formally closed and tagged complete (2026-08-05)** — both Critical tasks implemented, verified, documented, committed, and independently re-verified (see Sprint 1 Final Verification below). **Sprint 2 formally closed and tagged complete (2026-08-05)** — all six actionable High-priority tasks implemented (S2-T2/founder photo correctly held as blocked), verified, documented, committed, and independently re-verified (see Sprint 2 Final Verification below). **A fresh, independent platform-wide audit + self-challenge review was then run (2026-08-05, see `INDEPENDENT_PLATFORM_AUDIT_2026-08-05.md`) and its findings folded into an updated Sprint 3 plan below** — Sprint 3 is now fully specified (Critical/High/Medium/Low, per-task Priority/Effort/Dependencies/Risk/Impact/Acceptance-criteria/Validation/Rollback) but **not started**; Sprint 4 remains summary-only. Neither may begin without your explicit approval — see `SPRINT2_EXECUTIVE_COMPLETION_REPORT.md` for the full executive summary of everything to date, including the still-unresolved question of whether/when to push and deploy Sprint 1+2's 7 local-only commits.
**Working method (binding for every task, not just Sprint 1):** implement → verify functionality → update documentation → commit → only then move to the next task. No batching multiple tasks into one commit.

---

## Sprint 1 — Critical (must complete before launch)

### S1-T1: Fix self-referencing "Related" content across Portfolio, Journal, and Workshops
- **Objective:** A related-content section must never link back to the page the visitor is already on. Fix the root cause (no self-exclusion in the related-items filter) everywhere the same pattern exists, not just on the one page the review happened to catch it on.
- **Affected files:**
  - `src/app/work/[slug]/page.tsx` (lines ~77 `relatedProjects` filter, ~87 `relatedWorkshops` filter)
  - `src/app/journal/[slug]/page.tsx` (lines ~91-92 related-items filter)
  - `src/app/workshops/[slug]/page.tsx` (line ~64 related-items filter)
- **Dependencies:** None — self-contained logic fix, no schema or content change required.
- **Estimated implementation effort:** Low.
- **Expected business/user impact:** High — removes a visibly broken moment on the site's flagship, most-scrutinized page; prevents the identical bug from resurfacing silently the instant any other entity references itself or a small content set creates a similar collision.
- **Acceptance criteria:**
  - On `/work/sampson-sadia-wedding`, "Related Projects" no longer appears (zero *other* projects exist) — confirmed by absence of the section, not an empty grid.
  - Filter logic in all three files excludes the current item's own `id` from its related-items list.
  - No regression to the case where genuine, distinct related items exist (verified by temporarily reasoning through the filter logic, since production has no second item to test against live).
  - `npm run build` and `npm run lint` pass.

### S1-T2: Replace generic "no results" empty states with an honest "Coming Soon" treatment when a section has zero total content
- **Objective:** Distinguish "nothing published in this section yet" from "your filter matched nothing" on Journal and Workshops, and present the zero-content case with the same honest, intentional framing already proven on Talent Management — not a bare message that reads like a broken search.
- **Affected files:**
  - `src/app/journal/page.tsx` (empty-state block, ~lines 223-226; `items` already holds the unfiltered total)
  - `src/app/workshops/page.tsx` (empty-state block, ~lines 95-98; `allWorkshops` already holds the unfiltered total)
- **Dependencies:** None — the unfiltered totals needed to distinguish the two cases are already fetched and in scope; no new data source required.
- **Estimated implementation effort:** Low.
- **Expected business/user impact:** High — directly addresses the review's Critical finding that 2 of 7 primary nav destinations currently read as empty/broken rather than "not published yet." Matches the platform's existing discipline (proven on Talent Management) of honest, non-defensive messaging about what isn't ready rather than either faking content or leaving a bare "no results."
- **Acceptance criteria:**
  - When total unfiltered content is zero, Journal and Workshops render a distinct "Coming Soon"-style message (not the filter-specific "no matches" copy), visually consistent with the rest of the page.
  - When content exists but the active filter matches nothing, the existing "no results for this filter" copy is preserved unchanged (this case must not regress).
  - No fabricated claims, dates, or specifics in the new copy — consistent with the platform's "never invent facts" content discipline.
  - `npm run build` and `npm run lint` pass; manual check on staging/local confirms both states render correctly.

### Sprint 1 Final Verification (2026-08-05)

Conducted at your request before opening Sprint 2. Six checks, each reported against what was actually found:

1. **Re-ran TypeScript, ESLint, and Production Build.** All three clean — `npx tsc --noEmit` zero errors, `npm run lint` zero errors (2 pre-existing warnings in `PortfolioProjectForm.tsx`, unrelated to Sprint 1), `npm run build` exit 0.
2. **Regression review of every file modified.** Full diff of both Sprint 1 commits reviewed line by line (`src/app/work/[slug]/page.tsx`, `src/app/journal/[slug]/page.tsx`, `src/app/journal/page.tsx`, `src/app/workshops/[slug]/page.tsx`, `src/app/workshops/page.tsx`). Every change is additive and narrowly scoped to the two Critical findings — no unrelated modifications.
3. **Duplicate-logic check.** Grepped the full `src` tree for every related-content field (`relatedProjectIds`, `relatedWorkshopIds`, `relatedPostIds`, `relatedArticleIds`) and their consuming components. Confirmed: no other page has the same unguarded self-reference pattern (`journal/authors/[slug]` and `workshops/instructors/[slug]` have no related-content logic at all). Also checked the Portfolio admin editor (`PortfolioProjectForm.tsx`) — it already excludes the current project from its "Related Projects" checkbox list via a pre-filtered `otherProjects` prop, so authoring-time self-selection was never possible there. Journal and Workshops content, by contrast, is authored directly in Sanity Studio with no equivalent guard — confirming the render-layer fix (§S1-T1) is the correct and necessary single point of defense for all three content types, not a duplicate of existing protection.
4. **Documentation/index synchronization.** `DOCUMENTATION_INDEX.md`, `PRODUCT_LAUNCH_READINESS_REVIEW.md`'s Launch Checklist, and this plan's own status line and Progress Log all cross-reference consistently as of this verification pass.
5. **Engineering Standards Manual compliance — one real deviation found and corrected.** `ENGINEERING_GUIDE.md` §5 ("UI Design Patterns: Loading, Empty, and Error States") documents an explicit, specific standard for empty states: either the section is absent, or — where an informational empty state is genuinely appropriate — "a plain, honest sentence." The original S1-T2 implementation instead built a richer eyebrow/headline/body/button block modeled on Talent Management's `isComingSoon` treatment. On closer reading, that precedent doesn't actually apply: Talent Management's copy is real, CMS-authored content on an existing `Service` document, not a client-side fallback for zero query results — so it isn't the same pattern at all. **Corrected**: both empty states were simplified to a single plain sentence with an inline text link (matching the existing `text-ordift-gold hover:text-ordift-gold-hover underline underline-offset-4` link convention used elsewhere, e.g. the Founder-page link on `/about`), removing the hardcoded eyebrow/headline copy and the now-unused `Button` import from both files.
6. **Simplification/refactor opportunities.** The §5 compliance fix above *was* the simplification opportunity — fewer lines, one fewer component dependency, no new visual pattern introduced. Re-reviewed both files after the fix: no further simplification identified. `S1-T1`'s filter fix remains a minimal one-line (or two-line, for readability) change with no further reduction possible.

**Re-verification after the correction:** `npx tsc --noEmit`, `npm run lint`, and `npm run build` all re-run and pass clean. Both non-regression paths (`/journal?q=zzz`, `/workshops?category=zzz`) re-checked live against the local dev server — correct "no results for this filter" copy still renders, zero console errors.

**Sprint 1 is formally closed.** Tag: `sprint-1-complete`.

---

## Sprint 2 — High Priority: Brand & Discoverability

**Approach:** every task below is evaluated from eight perspectives — Senior Software Engineer, UX/UI Designer, Creative Director, Brand Strategist, SEO Specialist, Accessibility Specialist, Marketing & Conversion Strategist, and (where applicable) Professional Photographer/Visual Director — before deciding the implementation, per your explicit instruction to implement the solution that best strengthens the brand rather than merely the technically correct one.

### S2-T1: Surface real portfolio work on its own department page
- **Objective:** Photography's department page shows three generic "imagery coming soon" placeholders even though a real, published Photography/Weddings project exists (`Service.slug` and `PortfolioProject.disciplines` share the exact same `PortfolioDiscipline` type — the data link already exists, nothing to invent).
- **Multidisciplinary evaluation:**
  - *Engineer:* trivial data-join — filter `getPortfolioProjects()` by `project.disciplines.includes(service.slug)`, no schema change.
  - *UX/UI Designer:* a department page whose own "Featured Work" doesn't show the one piece of proof directly relevant to it is a broken expectation — visitors scan department → proof, not department → generic art.
  - *Creative Director:* the placeholder treatment is a deliberate, premium "coming soon" system — it should stay in place as the graceful fallback for departments with zero matching work, not be treated as filler once real work exists.
  - *Brand Strategist:* a brand claiming to be a "connected system" undercuts itself if its strongest existing proof isn't connected to the department it belongs to.
  - *SEO:* internal links from a department page to its matching case study add topical relevance signal between `/services/photography` and `/work/sampson-sadia-wedding` that doesn't exist today.
  - *Accessibility:* real `PortfolioCard` already carries proper alt text and semantic markup; placeholders are decorative — this is a net accessibility improvement, not just cosmetic.
  - *Marketing/Conversion:* a department page with zero real proof asks for trust on faith; one real card next to the booking CTA is a meaningfully shorter path to conviction.
  - *Photography/Visual Director:* the fallback for zero matches must never crop or reuse assets outside their intended aspect ratio — reuse `PortfolioCard`/`ResponsiveImage` exactly as built for `/work`, no new image-handling logic.
- **Decision:** replace the 3 hardcoded `MediaPlaceholder` tiles with up to 3 real matching projects via `PortfolioCard`, sorted the same way `/work` sorts Featured Projects; fall back to the existing placeholder treatment only when zero projects match — never mix real cards and placeholders in the same grid (a visual inconsistency none of the eight lenses would sign off on).
- **Affected files:** `src/app/services/[slug]/page.tsx`.
- **Dependencies:** None.
- **Estimated effort:** Low.
- **Expected impact:** Medium-High.
- **Acceptance criteria:**
  - Photography's department page shows the real Sampson & Sadia project card, linking to `/work/sampson-sadia-wedding`.
  - Every other department (zero matching projects today) still shows the existing placeholder treatment unchanged — no regression.
  - No department ever mixes real cards and placeholder tiles in the same grid.
  - `npx tsc --noEmit`, `npm run lint`, `npm run build` pass; verified live on `/services/photography` and one placeholder-only department.

### S2-T3: Canonical tags on the remaining 11 routes
- **Objective:** Close the canonical-tag gap identified in the review — only 4 of ~15 routes set `alternates.canonical` today.
- **Multidisciplinary evaluation:**
  - *Engineer:* replicate the exact existing pattern from `work/page.tsx`/`legal/[slug]/page.tsx` — no new mechanism to design.
  - *SEO Specialist:* this is the single highest-leverage SEO fix in Sprint 2 — it removes ambiguity for search engines about which URL variant is authoritative for every major page type, not just the 4 already covered.
  - *Brand Strategist / Marketing:* indirect but real — inconsistent canonicalization dilutes search equity precisely as the studio starts actively linking this site in outreach and proposals.
  - *Accessibility / UX / Creative Director:* no user-facing surface at all — purely a `<head>`-level correctness fix, no visual or interaction risk.
- **Decision:** add `alternates.canonical` to all 11 remaining routes, each pointing at its own real, canonical URL (`SITE_URL` + path), consistent with the existing pattern.
- **Affected files:** `src/app/page.tsx`, `src/app/about/page.tsx`, `src/app/about/founder/page.tsx`, `src/app/services/page.tsx`, `src/app/services/[slug]/page.tsx`, `src/app/journal/page.tsx`, `src/app/journal/authors/[slug]/page.tsx`, `src/app/workshops/page.tsx`, `src/app/workshops/[slug]/page.tsx`, `src/app/workshops/instructors/[slug]/page.tsx`, `src/app/book/page.tsx`.
- **Dependencies:** None.
- **Estimated effort:** Medium (11 small, mechanical edits).
- **Expected impact:** Medium.
- **Acceptance criteria:** every one of the 11 routes renders a `<link rel="canonical">` pointing at its own correct absolute URL; dynamic routes (`services/[slug]`, `workshops/[slug]`, etc.) resolve per-instance, not to a static template; `npx tsc --noEmit`, `npm run lint`, `npm run build` pass; spot-checked live via DOM inspection on at least one static and one dynamic route.

### S2-T4: Site-wide Organization/WebSite structured data
- **Objective:** No route currently carries `Organization` or `WebSite` JSON-LD — the review flagged this as reducing eligibility for Google Knowledge Panel treatment and sitelinks.
- **Multidisciplinary evaluation:**
  - *Engineer:* make `RootLayout` an async Server Component, fetch `contentRepository.getSiteSettings()` once, render one shared `<script type="application/ld+json">` — memoization via the existing repository pattern, no new data-fetching mechanism.
  - *SEO Specialist:* `Organization` schema is the foundation Google needs to associate the domain with the business entity across search, Knowledge Panel, and rich results — the single most valuable technical-SEO fix available before launch.
  - *Brand Strategist:* this is the machine-readable equivalent of "how we introduce ourselves" — it should say exactly what the site itself says, nothing more confident than the copy already is.
  - *Accessibility / UX / Creative Director:* invisible to visitors, zero rendering risk.
  - *Marketing:* stronger, more complete search-result presentation (an accurate Knowledge Panel, sitelinks) compounds every other marketing effort that drives someone to search "Ordift Studios" by name.
- **Decision — discipline over ambition:** populate the schema only from fields that are real, CMS-backed data already flowing through `SiteSettings` (`siteName`, `logoUrl`, `contactEmail`, `socialLinks`) — never invent a street address, a founding date, or social profile URLs that don't exist. `sameAs` is included only when `socialLinks` is non-empty; the whole block degrades gracefully as more real data is added later, exactly the same "never invent facts" discipline this project has held to everywhere else.
- **Affected files:** `src/app/layout.tsx`.
- **Dependencies:** None (uses the existing `getSiteSettings()` repository method).
- **Estimated effort:** Medium.
- **Expected impact:** Medium-High.
- **Acceptance criteria:** every page renders one `Organization`/`WebSite` JSON-LD block sourced from real `SiteSettings` data; no hardcoded facts not already present in the CMS; `sameAs` omitted when `socialLinks` is empty; validates as well-formed JSON; `npx tsc --noEmit`, `npm run lint`, `npm run build` pass; verified live via DOM inspection.

### S2-T5: Branded default social-share image
- **Objective:** Every page without its own Open Graph image (everything except the one case study and legal pages) falls back to the raw 474×524 logo file — undersized, wrong aspect ratio, not a compelling shareable visual.
- **Multidisciplinary evaluation:**
  - *Engineer:* Next.js's file-convention `opengraph-image.tsx` (via `next/og`'s `ImageResponse`) generates a real 1200×630 image at build time from real brand assets — no new design tool, no manual export step, and it can never drift out of sync with the brand system since it's compiled from the same source of truth as everything else.
  - *Creative Director:* compose the real approved logo (`logo-full-gold.png`) centered on the real navy brand gradient already used on `/coming-soon` and every department hero — reusing exactly what's already approved, not inventing a new visual.
  - *Brand Strategist:* the share image is often a prospect's very first impression of the studio, before they've clicked through — it should look as premium as the site itself, not like a cropped logo.
  - *SEO / Marketing & Conversion:* social platforms (WhatsApp, LinkedIn, iMessage) render this image at full width in previews — a correct 1200×630 branded card measurably improves click-through on every link this studio shares.
  - *Accessibility:* `alt` text set via the file convention's `alt` export, describing the studio rather than left blank.
  - *Photographer/Visual Director:* not applicable — no photographic asset involved, purely logo + brand color composition.
- **Decision:** build `src/app/opengraph-image.tsx` (and a matching `twitter-image.tsx`, or a shared generator function) rendering the real logo on the real navy gradient at 1200×630; remove the static `images` array from the root layout's `openGraph`/`twitter` metadata so there's exactly one mechanism defining the default image (no ambiguity about precedence); pages with their own explicit OG image (the case study, legal pages) are unaffected since their more specific, page-level metadata already takes precedence.
- **Affected files:** `src/app/layout.tsx` (remove the static `images` field), new `src/app/opengraph-image.tsx` (and `twitter-image.tsx` if needed as a separate file, or shared via a common generator).
- **Dependencies:** None — uses only assets already in `/public/brand`.
- **Estimated effort:** Medium.
- **Expected impact:** Medium-High.
- **Acceptance criteria:** pages without their own OG image now serve a real 1200×630 branded PNG; the case study and legal pages' existing page-specific images are unaffected (verified, not assumed); `npx tsc --noEmit`, `npm run lint`, `npm run build` pass; verified live via DOM/network inspection that the generated image actually renders correctly at the right dimensions.

### S2-T7: Resolve the guaranteed-empty portfolio filter option
- **Objective:** `/work`'s category filter includes "Destination Weddings," which matches zero published projects today, producing a certain dead end for anyone who clicks it.
- **Multidisciplinary evaluation:**
  - *Engineer:* filter the rendered category-chip list down to categories with at least one matching published project — a pure presentation-layer fix, no schema change, and it self-heals the moment a Destination Weddings project publishes.
  - *UX/UI Designer:* a filter control should never advertise an option with a guaranteed-empty result — that's the definition of a UI dead end.
  - *Creative Director / Brand Strategist:* small, but consistent with the platform's broader discipline of never presenting something as ready when it isn't.
  - *Marketing/Conversion:* a wedding client filtering by destination and hitting nothing is a moment of doubt at exactly the point they were trying to self-qualify — removing the option removes the doubt.
  - *SEO / Accessibility:* neutral — fewer, more accurate filter controls if anything slightly improves both.
- **Decision:** compute the available category list from categories that actually have ≥1 matching project, rather than the full configured category list.
- **Affected files:** `src/app/work/page.tsx`.
- **Dependencies:** None.
- **Estimated effort:** Low.
- **Expected impact:** Low.
- **Acceptance criteria:** "Destination Weddings" (and any other zero-match category) no longer appears as a filter chip; "Weddings" (has a real match) still appears; selecting any visible chip never returns zero results; `npx tsc --noEmit`, `npm run lint`, `npm run build` pass; verified live.

### S2-T6: Case-study copy polish (production content, not code)
- **Objective:** Fix the grammar issues ("Unstable lighting from the Natural Sunlight...", "We had to use and repositioned...") and the near-duplicate gallery caption on the one live case study.
- **Multidisciplinary evaluation:**
  - *Creative Director / Brand Strategist:* this is the single most-scrutinized page on the site — every sentence should read as intentional, especially in a section titled "Challenges/Solution," which is exactly the part a discerning client reads most closely.
  - *Marketing/Conversion:* a visible typo/grammar slip in the flagship proof-of-work quietly undercuts the trust the rest of the page works to build.
  - *Photographer/Visual Director:* no image change needed — the caption fix removes a repeated line, it doesn't touch the gallery sequencing or selection itself.
  - *Engineer:* not a code change at all — this is Sanity-hosted production content, edited the same way the earlier "Unstable lightening" typo fix was handled in this engagement: a disposable QA admin account, created and used through the real Admin Portal's native editor, then deleted and independently reverified deleted.
  - *Accessibility / SEO:* neutral, no structural change — pure copy edit.
- **Decision:** use the same disposable-QA-account pattern already established and approved in this engagement, rather than any direct API/token route.
- **Affected files:** none in the repository — production Sanity content only (`portfolioProject` document, Sampson & Sadia Wedding).
- **Dependencies:** requires the disposable QA account creation/cleanup script pattern already used earlier this engagement.
- **Estimated effort:** Low.
- **Expected impact:** Low-Medium.
- **Acceptance criteria:** the Challenges/Solution paragraphs read grammatically correct with consistent capitalization; the duplicate "Love captured naturally" / "Love captured naturally." caption pair is resolved (one made distinct, or the exact duplicate removed); verified live on production `/work/sampson-sadia-wedding`; the disposable QA account is deleted and deletion independently reverified afterward, consistent with this project's standing operational discipline.

### S2-T2: Founder photo — blocked, not skipped
- **Objective:** the review's High-priority finding that `/about/founder` has no photo of Myredlive Anim-Tetey anywhere on the page.
- **Multidisciplinary evaluation:** every lens agrees this matters (Creative Director and Brand Strategist rate it High impact — a founder-led brand's most personal page with zero photo of the founder is a real trust gap for investor/employee/journalist/partner personas; Marketing/Conversion agrees a face-behind-the-brand is a meaningful trust signal) — but there is nothing to implement without a real photo.
- **Decision:** this cannot be done without a real, supplied photograph — fabricating or sourcing a stand-in image would violate this project's "never invent facts" discipline at a much higher stakes level than a copy edit. **Explicitly held as blocked**, not silently dropped, pending you supplying a photo asset. No code or content change made for this item in Sprint 2.
- **Affected files:** `src/app/about/founder/page.tsx` (once a photo exists).
- **Dependencies:** a real photograph of the founder, supplied by you.
- **Estimated effort:** Low, once unblocked.
- **Expected impact:** High.
- **Acceptance criteria:** N/A until unblocked — tracked here so it isn't lost, not closed as done.

### Sprint 2 Final Verification (2026-08-05)

Conducted at your request before opening Sprint 3, mirroring the Sprint 1 audit. Six checks, each reported against what was actually found:

1. **Re-ran TypeScript, ESLint, and Production Build.** All three clean — `npx tsc --noEmit` zero errors, `npm run lint` zero errors (same 2 pre-existing warnings in `PortfolioProjectForm.tsx`, unrelated to Sprint 2), `npm run build` exit 0 with all routes generated, including the new static `/opengraph-image` and `/twitter-image` routes.
2. **Regression review of every file modified.** Full diff of all four Sprint 2 code commits reviewed line by line (20 files, 318 insertions / 42 deletions) — `layout.tsx`, the new `opengraph-image.tsx` / `twitter-image.tsx` / `generateOgImage.tsx`, all 11 canonical-tag routes, `work/page.tsx`, `work/[slug]/page.tsx`, `journal/[slug]/page.tsx`, `legal/[slug]/page.tsx`, and `services/[slug]/page.tsx`. Every change is additive and scoped to its task; the `work/page.tsx` category-filter fix has one benign edge case (a category selected before switching disciplines can end up not rendered as a chip if it has zero results under the new discipline — the filter itself still works correctly and remains recoverable via "All Categories"), not a regression.
3. **Duplicate-logic check.** Two patterns checked: (a) the local per-page `const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || "https://ordiftstudios.com"` constant, present in every static-metadata page, versus the shared `siteUrl()` helper in `src/lib/shared/env.ts` — confirmed this dual pattern **predates Sprint 2** (`src/app/page.tsx` already had it before this sprint) and Sprint 2 correctly followed each file's existing convention rather than introducing a new one; not a regression. (b) The repeated `openGraph`/`twitter` metadata-object shape, found in the §19 fix below — flagged as a real simplification opportunity and logged as `S4-T6` rather than refactored now (see point 6).
4. **Documentation/index synchronization — one real gap found and corrected.** `DOCUMENTATION_INDEX.md` was already current. `PRODUCT_LAUNCH_READINESS_REVIEW.md`'s Launch Checklist, however, still had H-1, H-2, H-4/H-5, H-6, H-7, and H-8 unchecked despite all six being completed in Sprint 2 — the same checklist-update step that was done for C-1/C-2 after Sprint 1 had not been repeated. **Corrected**: all six now checked off with `Done 2026-08-05` annotations cross-referencing the specific Sprint 2 task, matching the existing C-1/C-2 format exactly; H-3 (founder photo) and M-1/M-2 (Sprint 3) left correctly unchecked.
5. **Engineering Standards Manual compliance — one real deviation found and corrected.** `ENGINEERING_GUIDE.md` §19 ("SEO Implementation Standards") requires every public page to set an explicit `openGraph` block *and* an explicit `twitter` block — never relying on the root layout's defaults falling through, specifically because Next.js does not auto-derive Twitter Card fields from `openGraph`. Ten pages touched by `S2-T3` (canonical tags) had only `alternates.canonical` added and still fell through to the root layout for `openGraph`/`twitter`: `about/page.tsx`, `book/page.tsx`, `services/page.tsx`, `workshops/page.tsx`, `about/founder/page.tsx`, `journal/page.tsx`, `services/[slug]/page.tsx`, `workshops/[slug]/page.tsx`, `workshops/instructors/[slug]/page.tsx`, `journal/authors/[slug]/page.tsx`. **Corrected**: each now sets an explicit `openGraph` (title/description/url/type/images) and `twitter` (card/title/description/images) block, `images` pointing at the site-wide `/opengraph-image` default established in `S2-T5` — required per the same live-tested precedence rule documented in `S2-T5`/`work/[slug]/page.tsx` (a page's own `openGraph` object without `images` does not inherit the root's file-convention image). Re-ran `npx tsc --noEmit`, `npm run lint`, `npm run build` — all clean.
6. **Simplification/refactor opportunities.** The now-13 pages setting an `openGraph`/`twitter` pair (the 10 above, plus `work/page.tsx`, `work/[slug]/page.tsx`, `journal/[slug]/page.tsx`, `legal/[slug]/page.tsx`) repeat the same object shape verbatim. A shared `buildPageMetadata({ title, description, canonical, images?, type? })` helper would remove that duplication. Not built now — introducing a new shared abstraction isn't part of a closure audit's fix budget, and the current per-page repetition is explicit and easy to read, not a correctness risk. **Logged as `S4-T6`** in the Sprint 4 backlog below rather than actioned mid-audit.

**Sprint 2 is formally closed.** Tag: `sprint-2-complete`.

---

## Sprint 3 — Updated Plan (2026-08-05, post-audit; NOT STARTED — awaiting your approval)

Superseded the original 4-item summary table below. Expanded after `INDEPENDENT_PLATFORM_AUDIT_2026-08-05.md` (a fresh, independent audit + self-challenge review run after Sprint 2's own closure audit) surfaced 13 additional findings beyond the three items `PRODUCT_LAUNCH_READINESS_REVIEW.md` originally scoped for Sprint 3. Every task below traces to a specific audit finding ID (`A-#`) or the original review. Grouped Critical → Future per your request; **nothing in this section has been implemented — this is the plan for your review only.**

### Prerequisite decision (not a Sprint 3 task — needs your call before any of this ships)

**Deployment.** All of Sprint 1 + Sprint 2 (7 commits, `sprint-1-complete`/`sprint-2-complete`) exist only on the local `main` branch — nothing has been pushed to `origin/main` or deployed to Vercel (`INDEPENDENT_PLATFORM_AUDIT_2026-08-05.md`, finding A-14). Sprint 3 can be implemented and verified locally regardless, but at some point a push/deploy decision needs to be made independent of Sprint 3's own scope. Flagging here so it isn't lost, not treating it as a Sprint 3 task since it's an operational action requiring your explicit go-ahead, not engineering work.

### Critical

None. No launch-blocking or data-integrity findings were surfaced by the fresh audit.

### High

**S3-T5 — Restore the Workshop `seo` field (TypeScript type ↔ Sanity schema mismatch) — ✅ Done 2026-08-06**
- **Finding:** `INDEPENDENT_PLATFORM_AUDIT_2026-08-05.md` A-3. Sanity's Workshop schema defines an `seo` field and the GROQ fragment fetches it, but the `Workshop` TypeScript type omits it entirely — editor-authored Workshop SEO input is fetched over the wire and silently discarded; `workshops/[slug]/page.tsx` hand-builds metadata from `shortDescription` instead.
- **Priority:** High. **Effort:** Low (add one field to one type, wire one `generateMetadata` branch — the same pattern already used for Service/PortfolioProject/JournalPost).
- **Dependencies:** none.
- **Risk level:** Low — additive type change, no existing behavior removed.
- **User impact:** Workshop pages currently show generic auto-generated metadata instead of whatever an editor actually wrote in Studio — affects search-result snippets and share cards for every workshop.
- **Technical impact:** Closes the one confirmed type/schema/adapter drift found across the three content types inspected; brings Workshop to parity with every other content type's SEO handling.
- **Acceptance criteria:** `Workshop.seo` exists in `src/lib/content/types.ts`, is populated by both the Sanity and local adapters, and `workshops/[slug]/page.tsx`'s `generateMetadata` prefers `workshop.seo.metaTitle`/`metaDescription`/`ogImageUrl`/`canonicalUrl` over the current hand-built fallback — matching the exact pattern already used in `services/[slug]/page.tsx`.
- **Validation checklist:** `tsc`/`lint`/`build` clean; live-check a workshop with Studio-authored SEO fields shows them in rendered `<head>`; a workshop with no SEO override still falls back to `shortDescription`-derived metadata (no regression for existing content).
- **Rollback:** single-commit revert; no data migration involved (Sanity documents already carry the field, only the read path changes).

**S3-T6 — Add branded `error.tsx` and `not-found.tsx` boundaries — ✅ Done 2026-08-06**
- **Finding:** A-10. Zero `error.tsx`/`not-found.tsx`/`global-error.tsx` exist anywhere in the repo; 18+ `notFound()` call sites across public dynamic routes all fall through to Next's bare default UI.
- **Priority:** High. **Effort:** Medium (one root `not-found.tsx`, one root `error.tsx`, both branded with `NavBar`/`Footer` and on-brand copy; a route-group-level override only if a specific route type needs different messaging).
- **Dependencies:** none.
- **Risk level:** Low — additive files, no existing route logic changes.
- **User impact:** High — this is the single most likely "feels unfinished" moment a visitor can trivially trigger (mistyped URL, stale bookmark, shared broken link) on an otherwise fully brand-consistent site.
- **Technical impact:** None beyond the new files; `error.tsx` must be a Client Component per Next's convention (documented in `node_modules/next/dist/docs`), everything else can stay server-rendered.
- **Acceptance criteria:** visiting any nonexistent path, or any `notFound()`-triggering slug, shows a branded 404 with navigation back into the site; a thrown error in a route shows a branded error state with a retry action, not Next's default overlay, in production mode.
- **Validation checklist:** manually trigger `notFound()` on `/work/does-not-exist`, `/journal/does-not-exist`, etc.; confirm `NavBar`/`Footer` render; confirm `prefers-reduced-motion`/heading-hierarchy standards are followed on the new pages too (don't create fresh §20 violations while fixing this).
- **Rollback:** delete the two new files; zero risk to existing routes.

**S3-T7 — Close remaining sitemap gaps (expands original S3-T1) — ✅ Done 2026-08-06**
- **Finding:** A-5, extends the original review's M-1. `/about/founder` and every `journal/authors/[slug]` page are missing from `sitemap.ts` despite being real, indexable, non-disallowed routes. (One initially-suspected gap, `workshops/instructors/[slug]`, was independently re-checked against `sitemap.ts` directly and confirmed **already present** — not part of this task.)
- **Priority:** High (was Medium in the original review; raised because it's now a confirmed 2-route gap, not 1, and directly SEO-adjacent to Sprint 2's own focus). **Effort:** Low.
- **Dependencies:** none — `contentRepository.getAuthors()` already exists and is already used elsewhere.
- **Risk level:** Low.
- **User impact:** Indirect (crawler discoverability only).
- **Technical impact:** None — purely additive to `sitemap.ts`'s existing `Promise.all`/entries pattern.
- **Acceptance criteria:** `/sitemap.xml` includes `/about/founder` and one entry per `journal/authors/[slug]`.
- **Validation checklist:** `curl localhost:3000/sitemap.xml` (dev) and grep for both; confirm no duplicate entries.
- **Rollback:** single-line revert per addition.

### Medium

**S3-T8 — Add JSON-LD to Workshop and Service detail pages — ✅ Done 2026-08-06 (promoted, see Medium reassessment below)**
- **Finding:** A-6. `workshops/[slug]/page.tsx` (bookable, date/price-bearing, has a live registration form) and `services/[slug]/page.tsx` have zero structured data, unlike `work/[slug]`, `journal/[slug]`, and `legal/[slug]`, which already have it.
- **Priority:** Medium. **Effort:** Medium (research correct `schema.org` type per page — `Event` or `Course` for workshops, `Service` for departments — then follow the existing `<script type="application/ld+json">` pattern already used three times in the codebase).
- **Dependencies:** none.
- **Risk level:** Low.
- **User impact:** Indirect (rich-result eligibility in search).
- **Technical impact:** Consistent with `ENGINEERING_GUIDE.md` §19's "structured data per entity type once it has a real detail page" standard.
- **Acceptance criteria:** both page types inject valid, Google Rich-Results-Test-passing JSON-LD.
- **Validation checklist:** validate output against Google's Rich Results Test or `schema.org` validator; confirm no duplicate/conflicting JSON-LD blocks on the same page.
- **Rollback:** remove the `<script>` block; no other code affected.

**S3-T9 — Design (not necessarily build) a content-revalidation strategy — ⏸️ Deferred to Sprint 4 (see Medium reassessment below)**
- **Finding:** A-1. No ISR/`revalidate`/`revalidateTag` and no Sanity webhook exist for any public content route — Studio edits require a full redeploy to go live.
- **Priority:** Medium (real operational gap, but current content volume is low enough that it hasn't caused a visible problem yet). **Effort:** Medium-High (two viable approaches — time-based `revalidate` per route, or a Sanity webhook → `revalidateTag` route — need a decision before implementation, not just a mechanical fix).
- **Dependencies:** a decision on approach; if webhook-based, requires a new `/api/revalidate` route with a shared-secret check.
- **Risk level:** Medium if rushed (wrong revalidate window either serves stale content or defeats caching entirely) — Low if scoped as "design + recommendation" first, "implement" second.
- **User impact:** None directly today; growing risk as more editors use Studio expecting near-live updates.
- **Technical impact:** Touches every public content-detail route's segment config.
- **Acceptance criteria (for the design step):** a written recommendation (time-based vs. webhook, and why) added to `ARCHITECTURE_DECISIONS.md`, before any code changes.
- **Validation checklist (for implementation, once approved):** edit a real Sanity document, confirm the public page reflects it within the chosen window/trigger without a full redeploy.
- **Rollback:** revert the segment-config changes; webhook route can be disabled via env var if built that way.

**S3-T10 — Fix `DeliverablesGallery.tsx` empty alt text — ✅ Done 2026-08-06 (promoted, see Medium reassessment below)**
- **Finding:** A-7. `alt=""` on meaningful, per-item client deliverable thumbnails — a real §20 violation on a client-facing (not admin-only) surface.
- **Priority:** Medium. **Effort:** Low.
- **Dependencies:** none.
- **Risk level:** Low.
- **User impact:** Direct — a client using a screen reader currently cannot distinguish between their own deliverable files.
- **Technical impact:** None beyond the one attribute.
- **Acceptance criteria:** each thumbnail's `alt` describes the specific deliverable (e.g. filename or a per-item description field, not a generic placeholder).
- **Validation checklist:** screen-reader spot check or DOM inspection confirming non-empty, non-duplicate `alt` per item.
- **Rollback:** trivial single-line revert.

**S3-T11 — Legal Suite documentation/config cleanup — ✅ Documentation part done 2026-08-06; env-var decision pending your input (see Medium reassessment below)**
- **Finding:** A-12. `FINAL_LAUNCH_CERTIFICATION.md`'s legal-pages section is stale (describes a pre-2026-08-04 draft state; the Legal Suite v1.0 publish was intentional and is correctly live). `LEGAL_PAGES_APPROVED` env var is no longer consumed by the actual indexing/robots logic, but its surrounding code comments still describe it as a live gate.
- **Priority:** Medium. **Effort:** Low (documentation + comment cleanup; optionally rewire the env var as a genuine kill-switch if you want one to still exist).
- **Dependencies:** a decision from you on whether `LEGAL_PAGES_APPROVED` should be (a) removed since it's vestigial, or (b) wired back in as a real safety switch.
- **Risk level:** Low.
- **User impact:** None (internal documentation accuracy only).
- **Technical impact:** Prevents a future engineer from trusting a control that no longer functions.
- **Acceptance criteria:** `FINAL_LAUNCH_CERTIFICATION.md` gets a superseded-pointer (matching the existing `BUSINESS_LAUNCH_AUDIT.md` pattern) for its legal-status claim; `env.ts`'s `LEGAL_PAGES_APPROVED` comment either reflects reality or the var is removed, per your decision.
- **Validation checklist:** grep confirms no remaining code path claims to be gated by a var that isn't actually checked.
- **Rollback:** documentation-only changes; trivially revertible.

**S3-T2 — Add Content Creation and Production Services to the footer — ✅ Fully done 2026-08-06 on both datasets, see final verification note below**
- **Priority:** Medium. **Effort:** Low. **Risk:** Low. **User impact:** Low-Medium (footer discoverability). **Acceptance criteria:** both services appear as footer links pointing at their existing `/services/[slug]` routes. **Validation:** visual + link-target check. **Rollback:** trivial.

**S3-T3 — Revisit "Talent" nav placement once the Talent directory ships** *(carried over, unchanged — still blocked on the Talent directory itself, Phase 1B)*
- **Priority:** Medium, but not actionable until its dependency ships. **Effort:** Low once unblocked. **Dependencies:** Talent directory (Phase 1B, not yet built).

### Medium Reassessment (2026-08-06, per your instruction not to auto-defer)

Per your directive, every Medium finding was evaluated individually — promote, implement partially, or defer — rather than blanket-deferred to Sprint 4:

| Task | Promote to now? | Justification |
|---|---|---|
| **S3-T8** (JSON-LD) | **Yes — implemented.** | Low-Medium effort, zero risk (additive `<script>` tag, same proven pattern as `work/[slug]`), directly improves production quality (rich-result eligibility), and doing it now while the Sprint 3 SEO context is loaded is more efficient than a later context-switch back into these same two files. |
| **S3-T10** (alt text) | **Yes — implemented.** | Trivial effort, real accessibility fix, zero risk, client-facing. No reason to defer a one-line fix. |
| **S3-T2** (footer links) | **Partially — local fixture done, live effect pending.** | Discovered mid-implementation (see correction note above) that this requires a live Sanity content edit, not a code change — the same category of action as `S2-T6`, which wasn't pre-scoped for this round. Low risk, low effort, but a live content change deserves your awareness before I make it, rather than bundling it in silently. **Recommend:** approve as a quick follow-up (same disposable-QA-account-via-Admin-Portal pattern already proven safe in Sprint 2), or fold into the next batch of content edits. |
| **S3-T11** (Legal Suite docs) | **Partially — documentation done, env-var decision deferred to you.** | The stale-certification-doc correction is pure documentation, zero risk, done now. The `LEGAL_PAGES_APPROVED` env var's fate (remove it since it's vestigial, or rewire it as a real kill-switch) is a genuine architectural/business decision, not an engineering judgment call — deferring the *decision* (not the whole task) to you is the correct call per this project's standing discipline around business decisions. |
| **S3-T9** (revalidation strategy) | **No — deferred to Sprint 4, decision documented now.** | This is the one Medium item that materially changes production *architecture* (every content-detail route's caching model) rather than adding a self-contained fix. Rushing an ISR/webhook decision inside a "clear Sprint 3, stay disciplined" pass risks exactly the kind of regression this whole engagement has been careful to avoid — it deserves its own dedicated implementation pass with full regression coverage across all 5 affected route types, not a bolt-on. Recommend scheduling it as the first item of Sprint 4 rather than squeezing it into Sprint 3's tail. |
| **S3-T3** (Talent nav) | **No — cannot be promoted.** | Hard dependency on the Talent directory (Phase 1B), which doesn't exist yet. Not a prioritization choice — there is nothing to implement. |

### Low

**S3-T12 — Anchor `HOLDING_PAGE_ALLOWLIST` matching to path segments**
- **Finding:** A-2. `src/proxy.ts:79`'s `startsWith(path)` has no segment boundary — a future route like `/apiary` would silently bypass the holding-page gate. No live impact today.
- **Priority:** Low. **Effort:** Low (change to segment-anchored matching, e.g. `pathname === path || pathname.startsWith(path + "/")`).
- **Risk level:** Low. **User impact:** None today; prevents a latent future bug. **Acceptance criteria:** existing allowlisted paths still work; a synthetic colliding path (test-only) is correctly NOT allowlisted. **Rollback:** trivial.

**S3-T13 — Add explicit `robots: { index: false }` to `/studio`**
- **Finding:** A-4. Studio relies solely on `robots.txt`'s disallow, unlike every other internal surface.
- **Priority:** Low. **Effort:** Low (one `metadata` export, matching the pattern already used on 18 admin pages). **Risk:** Low. **Rollback:** trivial.

**S3-T14 — Close the remaining raw `<img>` gap (non-`DeliverablesGallery` instances)**
- **Finding:** A-13 (excluding `DeliverablesGallery`, tracked separately as S3-T10 for its higher severity). `PortfolioProjectForm.tsx:231,1021` and `ProfileQuickCard.tsx:81` — 3 more raw `<img>` instances beyond the 2 previously-disclosed exceptions, all admin-only.
- **Priority:** Low (admin-only surfaces, real images not blob previews, but not client-facing). **Effort:** Low-Medium (swap to `ResponsiveImage` or add to the disclosed-exception list with a comment, per file). **Rollback:** trivial per file.

**S3-T15 — Extend `prefers-reduced-motion` coverage to hover/transition animations**
- **Finding:** A-8. `Button.tsx`, `DepartmentCard.tsx`, `ProfileQuickCard.tsx`'s hover-lift and slide-in animations have no `motion-reduce` variant; no global CSS fallback exists.
- **Priority:** Low. **Effort:** Low (add `motion-reduce:transition-none`/`motion-reduce:transform-none` variants, or one global `@media (prefers-reduced-motion: reduce) { * { transition: none !important; animation: none !important; } }` fallback in `globals.css` — the latter is lower-effort and covers future components too). **Rollback:** trivial.

**S3-T16 — Make Turnstile verification fail closed, not open**
- **Finding:** A-11. `verifyTurnstileToken()` returns `true` (accepts) if `TURNSTILE_SECRET_KEY` is unset. Confirmed not a live issue (both keys are set in production per `vercel env ls`), but the fail-open design is inconsistent with the staging Basic Auth gate's fail-closed behavior for a comparable "is this security control configured?" check.
- **Priority:** Low (defense-in-depth, not an active gap). **Effort:** Low (flip the early-return, or emit a loud server-side warning/alert instead of silently passing). **Risk:** Medium if done carelessly — must not accidentally block real production traffic if the env var name is ever typo'd; recommend an explicit `NODE_ENV`/`SITE_ENV` check so local dev without the key still works. **Rollback:** trivial revert.

**S3-T17 — Add `Strict-Transport-Security` header**
- **Finding:** performance/security audit pass, not independently numbered above. No HSTS header set anywhere (`next.config.ts` or `src/proxy.ts`).
- **Priority:** Low (Vercel's platform-level TLS still protects the connection; HSTS is defense-in-depth against protocol-downgrade attacks specifically). **Effort:** Low (one line in `next.config.ts`'s existing `SECURITY_HEADERS` object). **Rollback:** trivial.

## Sprint 4 — Future Enhancements (not started — planned only)

| # | Task | Effort | Impact |
|---|---|---|---|
| S4-T1 | Competitive benchmarking pass against world-class creative-agency sites | Medium | Medium |
| S4-T2 | Dedicated WCAG accessibility audit (contrast, screen-reader flow, focus order) | Medium | Medium |
| S4-T3 | Gallery lightbox, richer per-project JSON-LD, caption-authoring guidance, dedicated homepage hero visual (already tracked in `PRODUCT_ROADMAP.md`) | Medium | Medium |
| S4-T4 | Full Portfolio/Journal/Workshops content population (Phase 3) | High | High |
| S4-T5 | Rewrite the ~7 exact-duplicate gallery captions on Sampson & Sadia Wedding (found 2026-08-05 during S2-T6) — the public site's existing dedup logic hides the repetition from visitors, but the underlying content still needs real, photo-specific captions per image rather than one caption reused across many | Medium (needs the actual photos reviewed, not guessable from data alone) | Low-Medium |
| S4-T6 | Extract a shared `buildPageMetadata()` helper for the `openGraph`/`twitter` object shape now repeated verbatim across 13 page files (found 2026-08-05 during Sprint 2 Final Verification) | Low | Low (readability/maintainability only, no user-facing change) |
| S4-T7 | Scope `SANITY_API_TOKEN` to read-only where possible, or split into separate read/write tokens (found 2026-08-05, `INDEPENDENT_PLATFORM_AUDIT_2026-08-05.md`) — currently one token used for both reads and writes | Low | Low (defense-in-depth, no active vulnerability) |
| S4-T8 | Adopt the `server-only` npm package on write-token modules (`src/lib/supabase/admin.ts`, `src/sanity/lib/client.ts`) so a future accidental client-component import build-fails instead of relying solely on the "no `use client`" convention (found 2026-08-05) | Low | Low (defense-in-depth) |
| S4-T9 | Decide the fate of `src/lib/content/local/` (6 files) — confirmed dead code, zero consumers since the 2026-07-24 Sanity flip (found 2026-08-06 during S3-T5 live verification): either delete it, or wire it into the test suite as offline fixtures if that was the original intent | Low (delete) / Medium (wire into tests) | Low (maintainability/clarity only) |

---

## Progress Log

- 2026-08-05 — Plan created. Sprint 1 approved; implementation beginning with S1-T1.
- 2026-08-05 — **S1-T1 complete.** Fixed self-reference in `src/app/work/[slug]/page.tsx` (`relatedProjects`), `src/app/journal/[slug]/page.tsx` (`relatedPosts`, `relatedArticles`), and `src/app/workshops/[slug]/page.tsx` (`relatedWorkshops`) — each now excludes the current item's own id. Cross-type related-content filters (a project referencing a workshop, a journal post referencing a project) were left unchanged: different content types have distinct ids, so self-reference is structurally impossible there. Verified via local dev server against the richer local fixture dataset (which has multiple items per type, unlike production's single portfolio project) — related-content sections on `/work/sample-atelier-fashion-editorial`, `/journal/sample-five-lighting-setups`, and `/workshops/sample-portrait-lighting-workshop` all correctly show *other* items, not themselves, with zero console errors. `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass. Commit `dbe6524`.
- 2026-08-05 — **S1-T2 complete.** `src/app/journal/page.tsx` and `src/app/workshops/page.tsx` now distinguish "nothing published yet" (`items.length === 0` / `allWorkshops.length === 0`) from "your filter matched nothing" (existing `filtered.length === 0` / `upcoming.length === 0` messages, unchanged). The zero-total case renders an honest "Coming Soon" block — eyebrow, headline, body copy, and a "Get in Touch" button to `/book?service=general` — matching the visual pattern already proven on Talent Management's department page (`src/app/services/[slug]/page.tsx`), rather than a bare "no results" message. No fabricated claims or dates in the copy. Verified locally: the pre-existing "no results for this filter" path still renders correctly and unchanged (`/journal?q=zzz`, `/workshops?category=zzz`, zero console errors); the new zero-total path was verified by code review and the identical, already-proven conditional pattern rather than a live local render, since local fixtures always have content in both sections (only production is currently empty). `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass.
- 2026-08-05 — **S2-T1 + S2-T7 complete.** `src/app/services/[slug]/page.tsx` now surfaces up to 3 real matching portfolio projects (`project.disciplines.includes(service.slug)`) via `PortfolioCard` in Featured Work, falling back to the existing placeholder treatment only when zero projects match — verified live: Photography shows 3 real project cards, Production Services (zero matches) still shows placeholders, Talent Management (isComingSoon) is unaffected. `src/app/work/page.tsx`'s category filter chips now only render categories with ≥1 project matching the current discipline filter — verified live against local fixtures: 6 of 12 configured categories were correctly hidden for having zero matches. Zero console errors on either page. `npx tsc --noEmit`, `npm run lint`, `npm run build` all pass.
- 2026-08-05 — **S2-T3 complete.** Added `alternates.canonical` to all 11 remaining routes (`page.tsx`, `about/page.tsx`, `about/founder/page.tsx`, `services/page.tsx`, `services/[slug]/page.tsx`, `journal/page.tsx`, `journal/authors/[slug]/page.tsx`, `workshops/page.tsx`, `workshops/[slug]/page.tsx`, `workshops/instructors/[slug]/page.tsx`, `book/page.tsx`), matching the existing pattern from `work/page.tsx`. Verified live via DOM inspection on one static route (`/about/founder`) and two dynamic routes (`/services/photography`, `/workshops/sample-portrait-lighting-workshop`) — each resolves its own correct per-instance canonical URL, zero console errors. `npx tsc --noEmit`, `npm run lint`, `npm run build` all pass.
- 2026-08-05 — **S2-T4 + S2-T5 complete** (implemented together — both touch `src/app/layout.tsx`). **S2-T4:** `RootLayout` is now an async Server Component fetching `contentRepository.getSiteSettings()` once and rendering one shared `Organization` + `WebSite` JSON-LD block, sourced entirely from real CMS data (siteName/logoUrl/contactEmail/socialLinks) — `sameAs` omitted when `socialLinks` is empty rather than guessed at. **S2-T5:** built `src/app/opengraph-image.tsx` + `twitter-image.tsx` (Next's file-convention, via `next/og`'s `ImageResponse`) compositing the real gold logo on the real navy brand gradient at 1200×630, replacing the raw undersized logo fallback; removed the static `images` array from the root's metadata so this is the single source of the default. **Bug found and fixed during verification:** confirmed live that a page's own `openGraph`/`twitter` object fully replaces the root's — Next does not merge in the root's default image for just a missing `images` field, so any page with its own openGraph object but no image would show *no* share image at all rather than falling back. This affected `work/[slug]/page.tsx` (projects with no hero image yet), `journal/[slug]/page.tsx` (both the journalPost and pulseArticle branches, which had also been missing `title`/`description`/`url`/`type` entirely — a separate, real pre-existing gap, fixed alongside), and `legal/[slug]/page.tsx`'s enterprise-document branch. All three now explicitly fall back to `${siteUrl}/opengraph-image` when no real image exists, and journal's two branches now set complete, explicit openGraph/twitter blocks. Verified live across every affected page type: homepage (inherits root default), a real Photography department page (inherits), a portfolio project with no hero image (explicit fallback), a journal post (explicit fallback, previously-missing og:title/og:url now present), `/legal/privacy` (explicit fallback), and the case-study pattern generally (a page with a real image would still show its own — confirmed via code path, not directly testable locally since local fixtures have no real hero images). `npx tsc --noEmit`, `npm run lint`, `npm run build` all pass; zero console errors on every page checked.
- 2026-08-05 — **S2-T2 remains blocked, confirmed at Sprint 2 close.** No real photograph of Myredlive Anim-Tetey exists in this repository or in Sanity content — nothing was fabricated as a stand-in. Tracked here, not silently dropped; unblock by supplying a real photo asset whenever ready, no code change needed on this end once one exists.
- 2026-08-05 — **S2-T6 complete.** Fixed via the disposable-QA-account pattern through the real Admin Portal native editor on production `/admin/portfolio/dHIt4AvkoimGdzNqYoxv2V/edit`: the Challenges/Solution grammar (capitalization, non-parallel verbs) and the near-duplicate "Love captured naturally"/"Love captured naturally." caption pair — which turned out to be **three** instances (two exact duplicates of the punctuated version, not two as the original review's dedup-masked view suggested), now three distinct, honest captions. Both saves verified persisted after a full page reload (not just local state), and the project's `status` remained `Published` throughout (confirmed the "case-study"/"media" save scopes never touch the status field before using them). **Real finding, not fixed in this task:** while editing, the raw gallery data showed far more exact-duplicate captions than the live site displays (`activity_log`'s row count for this project's gallery revealed ~35 images, several sharing one caption 5-7 times each) — the public `Gallery.tsx` component's existing first-occurrence dedup logic (built during the earlier presentation review) was successfully hiding this from visitors, but it's masking a real content-authoring gap, not solving it. Recorded as a new Future Enhancement below rather than fixed here — out of this task's approved scope and would require seeing the actual photos to write honest, non-generic captions rather than inventing descriptions.
  **Operational discovery — the disposable-QA-account pattern needed one adjustment:** the standard "delete the account afterward" step failed with a 500 from Supabase's Auth admin API. Root cause: `activity_log.actor_user_id references public.profiles (id)` has no `on delete cascade`/`set null` — a deliberate audit-integrity protection (0004_admin_platform.sql) — and this QA account had performed two logged "updated" actions, so deleting it would have orphaned those audit rows, which Postgres correctly refused. **This is by design, not a bug** — no prior disposable QA account in this engagement had performed a logged write before being deleted, so this hadn't surfaced yet. Correct cleanup for an account with real audit history: revoke all `user_roles` (deletes cleanly, cascades normally) and ban the account indefinitely (`ban_duration`) as a second independent layer, leaving the now-powerless account and its legitimate audit trail intact rather than force-deleting either. Verified both independently: `user_roles` reverified empty via direct query, and — more importantly — attempted a real login with the account's actual credentials afterward and confirmed "Invalid email or password." All five disposable script files deleted afterward, none left in the repo. **This refines the established disposable-QA-account discipline for future sessions: check whether the account performed any logged action before assuming deletion will succeed; if it did, revoke roles + ban instead of deleting.**
- 2026-08-05 — **Sprint 1 Final Verification.** Full 6-point audit performed before opening Sprint 2 (see "Sprint 1 Final Verification" under Sprint 1 above for the complete write-up). One real finding: the S1-T2 empty-state treatment didn't match `ENGINEERING_GUIDE.md` §5's documented "plain, honest sentence" standard for empty states — the Talent Management precedent it was modeled on turned out to be CMS-authored content, not a comparable client-side empty-state fallback. Corrected in place: both empty states simplified to a plain sentence with an inline text link, matching the existing link convention used on `/about`; removed the now-unused `Button` import from both files. Re-ran `npx tsc --noEmit`, `npm run lint`, `npm run build` (all clean) and re-verified both non-regression paths live with zero console errors. No other duplicate logic, documentation drift, or simplification opportunities found. **Sprint 1 formally closed and tagged `sprint-1-complete`.**
- 2026-08-05 — **Sprint 2 Final Verification.** Full 6-point audit performed before opening Sprint 3 (see "Sprint 2 Final Verification" under Sprint 2 above for the complete write-up). Two real findings, both corrected: (1) `PRODUCT_LAUNCH_READINESS_REVIEW.md`'s Launch Checklist hadn't been updated to check off Sprint 2's six completed High-priority items — corrected with `Done 2026-08-05` cross-references matching the existing C-1/C-2 format; (2) ten pages touched by `S2-T3` were missing the explicit `openGraph`/`twitter` blocks `ENGINEERING_GUIDE.md` §19 requires of every page, relying on root-layout fallthrough instead — corrected by adding explicit blocks (title/description/url/type/images) to `about/page.tsx`, `book/page.tsx`, `services/page.tsx`, `workshops/page.tsx`, `about/founder/page.tsx`, `journal/page.tsx`, `services/[slug]/page.tsx`, `workshops/[slug]/page.tsx`, `workshops/instructors/[slug]/page.tsx`, and `journal/authors/[slug]/page.tsx`, each pointing `images` at the `S2-T5` default `/opengraph-image` per the same precedence rule documented there. One duplicate-logic pattern reviewed and confirmed pre-existing/non-regressive (the local `SITE_URL` const vs. shared `siteUrl()` helper split), and one genuine simplification opportunity identified but deliberately not actioned mid-audit — logged as `S4-T6` (a shared `buildPageMetadata()` helper for the now 13x-repeated openGraph/twitter shape). Re-ran `npx tsc --noEmit`, `npm run lint`, `npm run build` after every correction — all clean throughout. **Sprint 2 formally closed and tagged `sprint-2-complete`.**
- 2026-08-05 — **Independent Platform Audit + Self-Challenge Review (post-Sprint-2, pre-Sprint-3).** Per your explicit instruction to not assume Sprint 2 complete just because its own tasks were marked done, ran a fresh, independent audit (four parallel research passes plus direct source verification of every claim before inclusion) across the full platform — architecture, routing, middleware, navigation, CMS/Sanity schemas, dynamic/static routes, SEO/metadata/JSON-LD/OG/Twitter/canonicals, accessibility, forms, error/loading/empty states, authentication, Admin Portal, Booking workflow, Portfolio workflow, Legal Suite, Workshop/Journal/Talent/Services pages, image optimization, caching, build output, console, TypeScript/ESLint/build, and deployment readiness. Full write-up: `INDEPENDENT_PLATFORM_AUDIT_2026-08-05.md`. Fourteen findings (A-1 through A-14), most Low/Medium severity, none launch-blocking. Two notable: **A-3** (High) — the `Workshop` TypeScript type is missing the `seo` field that Sanity's schema defines and the GROQ query fetches, so editor-authored Workshop SEO input is silently discarded; flagged as the one gap that arguably should have been caught during Sprint 2 itself, since it sits squarely in that sprint's own SEO focus. **A-14** (operationally significant, not a code defect) — confirmed via `git status`/`git log origin/main..HEAD` that all 7 Sprint 1+2 commits remain local-only, never pushed or deployed. One inter-agent factual conflict (sitemap coverage of `workshops/instructors/[slug]`) was caught and resolved by reading `sitemap.ts` directly rather than reported as-is. A companion self-challenge section (assumptions, hidden edge cases, duplicated logic, unnecessary complexity, inconsistencies, simplification opportunities, maintenance risk, technical debt, scalability) is included in the same document. All findings folded into an expanded Sprint 3 plan below (Critical/High/Medium/Low groupings, full Priority/Effort/Dependencies/Risk/Impact/Acceptance-criteria/Validation/Rollback per task) and two new Sprint 4 backlog items (`S4-T7`, `S4-T8`). A parallel `SPRINT2_EXECUTIVE_COMPLETION_REPORT.md` was produced covering Sprint 2 end-to-end. **No code was changed during this audit — findings only, per your explicit instruction to wait for approval before implementation.**
- 2026-08-06 — **All three High-severity Sprint 3 findings resolved (S3-T5, S3-T7, S3-T6), per your instruction to clear every High-severity item before any new feature work.** Implemented, verified, and documented individually:
  - **S3-T5 (A-3) complete.** Added `seo: SeoFields` to the `Workshop` type (`src/lib/content/types.ts`). No Sanity-adapter code change was needed — `client.fetch<Workshop[]>(workshopsQuery)` was already returning the field at runtime (the GROQ fragment already fetches it via `seoFragment("seo")`, which `coalesce()`s to a well-formed default); the field was simply invisible to TypeScript until now, exactly as the audit finding described. The local adapter's 4 workshop fixtures (`src/lib/content/local/data.ts`) needed real values added: the first fixture got a real `[SAMPLE]` SEO override (to demonstrate the override path renders), the other three got explicit `null` values (to demonstrate the fallback path still works — realistic, since not every editor fills in every optional field). `workshops/[slug]/page.tsx`'s `generateMetadata` now prefers `workshop.seo.metaTitle`/`metaDescription`/`canonicalUrl`/`ogImageUrl` over its hand-built fallbacks, matching the `services/[slug]/page.tsx` precedent — extended slightly further than that precedent by also honoring `canonicalUrl`/`ogImageUrl` overrides (matching Journal's pattern instead), since both fields were already being fetched and doing otherwise would have been a partial, inconsistent fix.
  - **S3-T7 (A-5) complete.** `src/app/sitemap.ts`: added `/about/founder` to `STATIC_ROUTES`; added `contentRepository.getAuthors()` to the parallel fetch and a `journal/authors/${slug}` mapping to the entries array, mirroring the existing `instructors` pattern exactly. `workshops/instructors/[slug]` was re-confirmed already present (not a gap) before making any change, consistent with the audit's own resolved-conflict note.
  - **S3-T6 (A-10) complete.** Created `src/app/not-found.tsx` (Server Component, on-brand navy/gold styling matching the rest of the site, `NavBar`+`Footer`, two recovery CTAs) and `src/app/error.tsx` (`"use client"`, using this Next.js version's `unstable_retry` prop — added in 16.2.0, the now-recommended recovery action over the older `reset()` — confirmed against `node_modules/next/dist/docs` rather than assumed from training data, per `AGENTS.md`'s standing warning that this Next version may differ). **One real nuance found during live verification, documented rather than silently accepted:** when `notFound()` is thrown from within a nested route segment (e.g. `work/[slug]/page.tsx`), the rendered *content* is correctly the new branded 404 (confirmed via `document.body.innerText` and screenshot) and Next's own automatic `<meta name="robots" content="noindex">` injection fires correctly (confirmed via DOM inspection) — but the browser tab `<title>` stays whatever the parent segment's already-resolved title was, not `not-found.tsx`'s own `metadata` export. This only happens for nested-segment throws; a genuinely unmatched URL (e.g. `/totally-fake-page-xyz`) gets the correct title. This is inherent Next.js App Router metadata-resolution order (the throwing page's own `generateMetadata` — which returns `{}` for a not-found case, per this codebase's existing `if (!x) return {};` convention — resolves before the throw), not a defect introduced here, and the SEO-critical noindex signal is unaffected. Live-verified across `/work/[bad-slug]` and `/services/[bad-slug]`, both consistent. `error.tsx` was verified via code-contract review against the Next 16.2 file-convention docs (correct props, `"use client"` present, `unstable_retry` wired) rather than a live-triggered fault — deliberately invasive fault-injection wasn't used to avoid destabilizing other verification in progress.
  - **Full verification across all three:** `npx tsc --noEmit`, `npm run lint` (0 errors, same 2 pre-existing warnings), `npm run build` (exit 0, 79 routes, `/_not-found` now a static route) all re-run clean after each fix and again after all three together. Live-verified on the local dev server: `/_not-found`'s standalone unmatched-URL path, `/work/[bad-slug]` and `/services/[bad-slug]`'s nested-notFound() path, zero console errors on any, no horizontal overflow at 375px or 1280px, single `<h1>` per page, "Back to Home" button functionally navigates. Sprint 3's High severity group is now fully closed — moving to the Medium-priority reassessment next, per your instruction to evaluate each on its own merits rather than blanket-defer.
- 2026-08-06 — **Correction, discovered while verifying S3-T5 live: `src/lib/content/index.ts` hardcodes `contentRepository` to `sanityContentRepository` (the live Sanity project, `staging` dataset per `NEXT_PUBLIC_SANITY_DATASET` — confirmed the public, non-secret env var directly), not the local fixture adapter.** This has been true since the 2026-07-24 CMS flip (task history #42) and is correctly documented in that file's own comment — but every place in this session's documentation (and, on review, likely in earlier sessions too) that described verification as being "against local fixtures" was actually describing the **staging Sanity dataset**, since `src/lib/content/local/` is never imported by anything except its own definition file — confirmed via `grep -rln "localContentRepository"` returning only `local/repository.ts` itself, zero consumers anywhere in `src/` or the test suite. This is a **documentation-accuracy correction, not a safety issue**: staging isolation was working exactly as designed the whole time (real production data was never at risk), and every "live-verified" claim in this session's Progress Log is still true as evidence that the code works — it was verified against real (staging) CMS data, which is if anything a stronger check than static fixtures would have been. Two concrete, immediate consequences now corrected:
  1. **S3-T5's local-fixture edits (`src/lib/content/local/data.ts`, 4 Workshop objects) have zero effect on the running app.** They were still necessary to make — `local/repository.ts` fully implements `ContentRepository`, so adding a required `seo: SeoFields` field to the `Workshop` type would have failed `tsc --noEmit` without them — but they don't demonstrate anything about live behavior. What was actually live-verified on `/workshops/sample-portrait-lighting-workshop` (a real staging document) is the **fallback path**: `workshop.seo.metaTitle`/`metaDescription` are empty on that real document, and `generateMetadata`/the new `Event` JSON-LD both correctly fell back to `workshop.title`/`shortDescription` without crashing — proving the code path is wired correctly end-to-end, just not proving the override path (no staging Workshop document currently has `seo` fields filled in to show that branch). The type fix itself is real and correct regardless.
  2. **S3-T2 is NOT actually resolved.** `src/lib/content/local/siteWideData.ts`'s `FOOTER_SETTINGS` edit (adding Content Creation / Production Services links) is equally inert. Checked the real, live (staging) footer directly (`Array.from(document.querySelectorAll('footer a'))`): it still lists only Photography/Videography/Graphic Design/Branding & Strategy — the two new links are not there. Fixing this for real requires a CMS content edit (Sanity `siteSettings.footerSettings`) via the Admin Portal, the same category of action as `S2-T6`, not a code change. **Not done yet — flagging for your decision** rather than unilaterally spinning up another disposable-QA-account session to make a live content edit that wasn't explicitly scoped as part of this "resolve High severity + reassess Medium" instruction.
  - **New technical-debt finding, not previously in the audit:** `src/lib/content/local/` (6 files: `data.ts`, `journalData.ts`, `portfolioData.ts`, `pulseData.ts`, `siteWideData.ts`, `repository.ts`) is entirely dead code — fully type-checked and maintained (as this session's own edits show) but never imported by the running app or the test suite. Logged as `S4-T9` in the Sprint 4 backlog below: either delete it (if truly unused) or wire it into the test suite as fast offline fixtures (if that was always the intent) — a decision for you, not made unilaterally here.
- 2026-08-06 — **S3-T2 fully resolved and independently verified on both datasets.** Sequence, each step confirmed rather than assumed:
  1. You published the footer edit through the live Vercel Production URL's `/studio` — landed in the `production` Sanity dataset (confirmed read-only: `_updatedAt: 2026-08-05T21:50:54Z`, 6 Services links in correct order).
  2. Read-only investigation (no writes) traced the discrepancy: `sanity.config.ts:12` resolves its dataset from the same `NEXT_PUBLIC_SANITY_DATASET` env var as the frontend, which is `"staging"` locally (`.env.local`) but was `"production"` via whatever URL you'd used. `CMS_MIGRATION.md`'s "What's next" section — written when the CMS was first connected — already documented the correct process for exactly this situation: real/approved content (naming **Footer** specifically) must be manually mirrored into both datasets, since "there's no automatic sync between datasets by design."
  3. You published the identical edit a second time through `localhost:3000/studio` (confirmed connected to `staging` per the same config), landing in the `staging` dataset.
  4. **Parity independently re-verified via a fresh read-only query** (not assumed from your report): both datasets' `footerSettings.columns` are now byte-identical (`JSON.stringify(staging.columns) === JSON.stringify(production.columns)` → `true`). Staging `_updatedAt: 2026-08-05T22:27:49Z`.
  5. **Phase 1 (full validation), performed against the local dev server (staging dataset):** all 16 footer links present with correct labels/hrefs, all 16 resolve `200 OK` (checked via `fetch()`, not just visual inspection); desktop (1280px)/tablet (768px)/mobile (375px) screenshots captured, zero horizontal overflow at any breakpoint; zero console errors; footer landmark is a real `<footer>` element with zero empty-text or missing-href links; site-wide `Organization`/`WebSite` JSON-LD unaffected and still parses correctly (footer was already architecturally confirmed to have no relationship to metadata/structured-data sources before making this change, per the earlier read-only investigation).
  6. **Phase 2 (Sprint 1–3 regression), performed live:** 22 routes spot-checked (`/`, `/about`, `/about/founder`, `/services` + all 7 department pages, `/work` + a project detail, `/journal` + a post, `/workshops` + a workshop detail, `/book`, 2 legal pages, `/sitemap.xml`, `/robots.txt`, both OG image routes, one deliberately-unmatched URL) — all correct status codes, zero server errors (`preview_logs` clean throughout), zero browser console errors on every page actually navigated to. Specifically reconfirmed still working post-footer-change: S1-T1's related-content self-reference fix (0 self-links on a workshop detail page), S2-T1's real Featured Work cards (3 `PortfolioCard` links still rendering on `/services/photography`), S2-T7's discipline-aware category filter (6 of 12 categories still correctly hidden), S3-T5's `Event` JSON-LD on the same workshop page, S3-T7's sitemap additions (`/about/founder` and `journal/authors/` entries both present, 37 total URLs), S3-T6's branded 404 (correct `404` status, correct branded copy, on a genuinely unmatched route), S3-T8's `Service` JSON-LD on `/services/photography`.
  7. **Mechanical re-verification:** `npx tsc --noEmit`, `npm run lint` (0 errors, same 2 pre-existing warnings), `npm run build` (exit 0, all routes) all re-run clean at the final state.
  8. **New finding, folded into the final Go/No-Go audit rather than acted on here:** grepped the full codebase for any analytics integration (`GA_MEASUREMENT_ID`, `gtag`, `@vercel/analytics`) — zero matches. This is a pre-existing, already-documented gap (`PRODUCT_ROADMAP.md`: "Google Analytics — no code built yet, needs a decision + measurement ID before it's worth building"), not something newly broken — confirmed, not rediscovered as new.
  **S3-T2 is now genuinely, fully complete — both datasets carry identical, correct footer content, independently verified rather than taken on your word alone, consistent with this project's standing verification discipline.**
